# STSInitialDeckAttrition — 项目计划书

> 初始卡组首场战损量化 · v0.4

---

## 1. 背景

游玩塔2 时，主观上觉得**第一场战斗**比塔1更容易掉血、掉得更多。  
想用游戏数据验证这个感受是否成立。

---

## 2. 目标

比较塔1与塔2：在**初始卡组**打**Act 1 弱怪池首场战斗**时，

- 「即使绝对最优出牌，也**必定吃战损**」的**加权**比例是否塔2更高；
- （可选）**加权期望战损**与战损分布是否塔2更差。

玩家模型：知晓牌序、**绝对最优决策**（对应 SL 下的理想操作）。

---

## 3. 做法

- **不与游戏本体联动**，不做真实游玩数据采集。
- 数据：初始卡组（你提供）+ 弱怪意图等（wiki / 已录入 JSON，格式见 `docs/游戏数据格式.md`）。
- 对每条**抽牌路线** ω（各回合上手组合 + 出现权重 w(ω)）求 **D(ω)** = 最优总战损。
- 汇总见 `docs/求解器设计.md` §1（**不等权**）。

---

## 4. 核心概念

| 用语 | 含义 |
|------|------|
| 战损 | 本场战斗失去的 HP；掉 1 点即计战损 |
| 抽牌路线 ω | 从开局到击杀，各回合上手组合的序列 |
| w(ω) | 该路线在洗牌规则下的概率权重（组合多重数乘积） |
| D(ω) | 该路线下最优出牌的总战损 |
| 必定吃战损 | D(ω) > 0 |
| 加权必伤概率 | ∑_{D>0} w(ω) / ∑ w(ω) |

---

## 5. 范围

**纳入：**

- 塔1 A20、塔2 A10（对应难度）
- 初始卡组（含进阶之灾）
- Act 1 弱怪池 · 首场战斗
- **各遭遇战单独记录**，不跨怪汇总

**技术文档：**

| 文档 | 内容 |
|------|------|
| `docs/游戏机制.md` | 战斗规则 |
| `docs/游戏数据格式.md` | JSON 目录与 schema |
| `docs/抽牌枚举协议.md` | 多重集抽牌 + 权重 + 协议档 |
| `docs/求解器设计.md` | DFS、层2 DP、剪枝、输出路线图 |
| `docs/角色战斗机制.md` | 六角色首场机制与译名 |
| `AGENTS.md` | 给其他 Agent 的快速上手指南 |

---

## 6. 首场试点

塔2 · **铁甲战士 vs 海洋混混**（A10，HP 47 / 48 / 49 各算一遍）。

数据：`data/sts2/`（格式见 `docs/游戏数据格式.md`）· 说明：`data/sts2/pilot_ironclad_vs_seapunk.md`。

备注：A10 下铁甲 vs 海洋混混已验证 **P(必伤)=1.0**（HP 47/48/49）。

---

## 7. 实现进度

| 项 | 状态 |
|----|------|
| 游戏机制、抽牌协议、角色机制文档 | 已文档化 |
| 层 2 战斗引擎 + 单回合剪枝 | 已实现（铁甲试点） |
| 路径内记忆化、内存修复 | 已实现 |
| 进度反馈 `--progress` | 已实现 |
| **DFS 打到击杀 + 加权统计** | 已实现（增量前沿 DP，`best_kill ≤ surv`） |
| JSON 加载器 | 已实现（`engine/load_data.py`） |
| 路线明细导出（JSONL） | 已实现（`--export`） |
| **角色/卡牌/遭遇 JSON** | 塔2 五角色 + Underdocks 弱怪 4 场已录；塔1 四角色已录；Overgrowth / 塔1 遭遇待录 |

试点结果（A10 · 铁甲 vs 海洋混混）：HP 47/48/49 均 **P(必伤)=1.0**、**加权期望战损 ≈ 7.58 / 7.74 / 8.91**、战损区间 **3–31**（最深停止回合 = 8）。

---

## 8. 待定

- [ ] 用户逐角色补充机制细节 → 更新 `docs/角色战斗机制.md` 与 `engine/`
- [ ] 敌人随机意图的处理方式（污泥旋转者）
- [ ] 塔1 弱怪遭遇录入与跑批
- [ ] Overgrowth 弱怪池录入
- [ ] 多角色 / 多遭遇求解器扩展（猎手 13 选 7、充能球、辉星、奥斯提、姿态…）
- [x] 导出格式（JSONL 全量，`--export`）
